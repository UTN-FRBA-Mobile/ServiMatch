## Reqs (o al menos con lo q me funciono)

- Python > 3.10
- pip instalado
- para instalar las dependencias correr: `pip install -r dependencias.txt`

> NOTA: el enpoint de monotononia todavia no sirve